let a: Int? = 5\nlet b: Int? = nil\nif let v = a { print(v) }\nif let v = b { print(v) } else { print(-1) }\nprint(a ?? 99)\nprint(b ?? 99)\nprint(a!)\nprint(a == nil)\nprint(b == nil)
func f(_ n: Int) -> Int? {\n  if n < 0 { return nil }\n  return n * 2\n}\nif let r = f(5) { print(r) }\nprint(f(-1) ?? -1)\nprint(f(3)!)
var x: Int? = 5\nx = 7\nprint(x ?? 0)\nx = nil\nprint(x ?? 0)
struct Box {\n  var v: Int?\n  var n: Int\n}\nvar b = Box(v: 3, n: 1)\nvar c = b\nc.v = nil\nprint(b.v ?? -1)\nprint(c.v ?? -1)\nprint(c.n)
func even(_ n: Int) -> Int? {\n  if n % 2 == 0 { return n }\n  return nil\n}\nvar i = 0\nvar s = 0\nvar t = 0\nwhile i < 6 {\n  if let v = even(i) { s = s + v }\n  t = t + (even(i) ?? 0)\n  i = i + 1\n}\nprint(s)\nprint(t)
func find(_ n: Int) -> Int? {\n  for i in 0 ..< 10 {\n    if i * i == n { return i }\n  }\n  return nil\n}\nprint(find(49) ?? -1)\nprint(find(50) ?? -1)\nif let r = find(36) { print(r * 2) }
let a: Int? = 3\nlet b: Int? = nil\nprint((a ?? 0) + (b ?? 10))\nprint((a ?? 0) * (a ?? 0))
func g(_ o: Int?) -> Int {\n  if let v = o { return v + 1 }\n  return 0\n}\nprint(g(41))\nprint(g(nil))\nlet a: Int? = 7\nprint(g(a))
enum Color { case red, green }\nfunc pick(_ c: Color) -> Int? {\n  if c == Color.red { return 1 }\n  return nil\n}\nprint(pick(Color.red) ?? -1)\nprint(pick(Color.green) ?? -1)
var best: Int? = nil\nvar i = 1\nwhile i < 6 {\n  if let b = best {\n    if i * i > b { best = i * i }\n  } else {\n    best = i * i\n  }\n  i = i + 1\n}\nprint(best ?? 0)
func half(_ n: Int) -> Int? {\n  if n % 2 == 0 { return n / 2 }\n  return nil\n}\nvar total = 0\nfor i in 1 ..< 9 {\n  total = total + (half(i) ?? 0)\n}\nprint(total)\nprint(half(8)!)
let a: Int? = 5\nvar n = 0\nif a != nil { n = 1 }\nif a == nil { n = 2 }\nprint(n)\nlet b: Int? = nil\nprint(b == nil)\nprint(b != nil)
struct Node {\n  var value: Int\n  var next: Int?\n}\nlet n1 = Node(value: 1, next: nil)\nlet n2 = Node(value: 2, next: 1)\nprint(n1.next ?? -1)\nprint(n2.next ?? -1)\nprint(n1.value + n2.value)
func classify(_ n: Int) -> Int? {\n  if n > 100 { return nil }\n  if n < 0 { return nil }\n  return n\n}\nvar ok = 0\nfor i in 0 ..< 5 {\n  if let v = classify(i * 40) { ok = ok + v }\n}\nprint(ok)
let a: Int? = 4\nlet b = a ?? 0\nlet c: Int? = b\nprint(c!)\nprint(c ?? -1)
