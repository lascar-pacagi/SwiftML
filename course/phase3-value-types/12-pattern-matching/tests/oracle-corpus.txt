enum Shape {\n  case circle(Int)\n  case rect(Int, Int)\n  case dot\n}\nfunc area(_ s: Shape) -> Int {\n  switch s {\n  case .circle(let r): return r * r\n  case .rect(let w, let h): return w * h\n  case .dot: return 0\n  }\n}\nprint(area(Shape.rect(3, 4)))\nprint(area(Shape.circle(5)))\nprint(area(Shape.dot))
enum Op {\n  case lit(Int)\n  case add(Int, Int)\n  case mul(Int, Int)\n}\nfunc eval(_ o: Op) -> Int {\n  switch o {\n  case .lit(let n): return n\n  case .add(let a, let b): return a + b\n  case .mul(let a, let b): return a * b\n  }\n}\nprint(eval(Op.lit(9)))\nprint(eval(Op.add(3, 4)))\nprint(eval(Op.mul(6, 7)))
func name(_ n: Int) -> Int {\n  switch n {\n  case 1: return 10\n  case 2: return 20\n  case 3: return 30\n  default: return 0\n  }\n}\nprint(name(1))\nprint(name(3))\nprint(name(9))
enum Light { case red, amber, green }\nfunc wait(_ l: Light) -> Int {\n  var s = 0\n  switch l {\n  case .red: s = 30\n  case .amber: s = 5\n  case .green: s = 0\n  }\n  return s + 1\n}\nprint(wait(Light.red))\nprint(wait(Light.amber))\nprint(wait(Light.green))
enum Token {\n  case num(Int)\n  case plus\n  case minus\n  case eof\n}\nfunc weight(_ t: Token) -> Int {\n  switch t {\n  case .num(let n): return n * 100\n  case .plus: return 1\n  default: return -1\n  }\n}\nprint(weight(Token.num(7)))\nprint(weight(Token.plus))\nprint(weight(Token.minus))\nprint(weight(Token.eof))
enum Step {\n  case up(Int)\n  case down(Int)\n  case stay\n}\nfunc apply(_ p: Int, _ s: Step) -> Int {\n  switch s {\n  case .up(let n): return p + n\n  case .down(let n): return p - n\n  case .stay: return p\n  }\n}\nvar pos = 0\nvar i = 0\nwhile i < 6 {\n  var s = Step.stay\n  if i % 3 == 0 { s = Step.up(i) }\n  if i % 3 == 1 { s = Step.down(1) }\n  pos = apply(pos, s)\n  i = i + 1\n}\nprint(pos)
enum E { case a(Int), b }\nvar t = 0\nlet e = E.a(5)\nswitch e {\ncase .a(_): t = 1\ncase .b: t = 2\n}\nprint(t)
enum Pair {\n  case both(Int, Int)\n  case none\n}\nfunc sum(_ p: Pair) -> Int {\n  switch p {\n  case .both(let a, let b): return a + b\n  case .none: return 0\n  }\n}\nprint(sum(Pair.both(20, 22)))\nprint(sum(Pair.none))
enum Color { case red, green, blue }\nfunc code(_ c: Color) -> Int {\n  switch c {\n  case .red: return 1\n  case .green: return 2\n  case .blue: return 3\n  }\n}\nvar total = 0\ntotal = total + code(Color.red)\ntotal = total + code(Color.green)\ntotal = total + code(Color.blue)\nprint(total)
enum Cmd {\n  case push(Int)\n  case pop\n}\nfunc run(_ acc: Int, _ c: Cmd) -> Int {\n  switch c {\n  case .push(let n): return acc + n\n  case .pop: return acc - 1\n  }\n}\nvar acc = 0\nfor i in 1 ..< 5 {\n  acc = run(acc, Cmd.push(i))\n}\nacc = run(acc, Cmd.pop)\nprint(acc)
enum Sign { case neg, zero, pos }\nfunc classify(_ n: Int) -> Sign {\n  if n < 0 { return Sign.neg }\n  if n == 0 { return Sign.zero }\n  return Sign.pos\n}\nfunc show(_ s: Sign) -> Int {\n  switch s {\n  case .neg: return -1\n  case .zero: return 0\n  case .pos: return 1\n  }\n}\nprint(show(classify(-7)))\nprint(show(classify(0)))\nprint(show(classify(7)))
func bucket(_ n: Int) -> Int {\n  switch n % 3 {\n  case 0: return 100\n  case 1: return 200\n  default: return 300\n  }\n}\nvar s = 0\nfor i in 0 ..< 6 {\n  s = s + bucket(i)\n}\nprint(s)
enum Tree {\n  case leaf(Int)\n  case node(Int, Int)\n}\nfunc total(_ t: Tree) -> Int {\n  switch t {\n  case .leaf(let v): return v\n  case .node(let a, let b): return a * 10 + b\n  }\n}\nprint(total(Tree.leaf(4)))\nprint(total(Tree.node(3, 7)))
enum Shape {\n  case circle(Int)\n  case dot\n}\nstruct Canvas {\n  var w: Int\n  var h: Int\n}\nfunc fits(_ s: Shape, _ c: Canvas) -> Bool {\n  switch s {\n  case .circle(let r): return r * 2 <= c.w && r * 2 <= c.h\n  case .dot: return true\n  }\n}\nlet c = Canvas(w: 10, h: 8)\nprint(fits(Shape.circle(3), c))\nprint(fits(Shape.circle(5), c))\nprint(fits(Shape.dot, c))
enum State { case start, middle, done }\nvar st = State.start\nvar steps = 0\nwhile steps < 4 {\n  switch st {\n  case .start: st = State.middle\n  case .middle: st = State.done\n  case .done: st = State.start\n  }\n  steps = steps + 1\n}\nprint(steps)\nprint(st == State.middle)
enum Maybe {\n  case some(Int)\n  case none\n}\nfunc orElse(_ m: Maybe, _ d: Int) -> Int {\n  switch m {\n  case .some(let v): return v\n  case .none: return d\n  }\n}\nprint(orElse(Maybe.some(41), 0) + 1)\nprint(orElse(Maybe.none, 7))
