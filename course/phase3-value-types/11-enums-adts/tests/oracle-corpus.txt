enum Color { case red, green, blue }\nlet c = Color.green\nprint(c == Color.green)\nprint(c == Color.red)
enum Dir: Int { case north, south, east, west }\nprint(Dir.north.rawValue)\nprint(Dir.south.rawValue)\nprint(Dir.east.rawValue)\nprint(Dir.west.rawValue)
enum D: Int {\n  case a\n  case b\n  case c\n}\nprint(D.a.rawValue + D.b.rawValue * 10 + D.c.rawValue * 100)
enum Light { case red, green }\nvar l = Light.red\nvar n = 0\nwhile n < 5 {\n  if l == Light.red { l = Light.green } else { l = Light.red }\n  n = n + 1\n}\nprint(l == Light.green)
enum Color { case red, green }\nfunc flip(_ c: Color) -> Color {\n  if c == Color.red { return Color.green }\n  return Color.red\n}\nprint(flip(Color.red) == Color.green)\nprint(flip(flip(Color.red)) == Color.red)
enum Suit: Int { case clubs, diamonds, hearts, spades }\nvar total = 0\nfor i in 0 ..< 4 {\n  total = total + i\n}\nprint(total)\nprint(Suit.spades.rawValue)
enum Color { case red, green, blue }\nlet a = Color.red\nlet b = Color.blue\nprint(a == b)\nprint(a != b)\nprint(a == a)
enum State: Int { case idle, running, done }\nfunc next(_ s: State) -> State {\n  if s == State.idle { return State.running }\n  if s == State.running { return State.done }\n  return State.done\n}\nvar s = State.idle\nprint(next(s).rawValue)\nprint(next(next(s)).rawValue)
enum Shape {\n  case circle(Int)\n  case rect(Int, Int)\n  case dot\n}\nfunc size(_ s: Shape) -> Int { return 1 }\nlet a = Shape.circle(5)\nlet b = Shape.rect(3, 4)\nlet c = Shape.dot\nprint(size(a) + size(b) + size(c))
enum Color { case red, green }\nstruct Pen {\n  var width: Int\n}\nlet p = Pen(width: 3)\nlet c = Color.green\nprint(p.width)\nprint(c == Color.green)
enum Dir: Int { case north, south, east, west }\nvar sum = 0\nfor i in 0 ..< 3 {\n  sum = sum + Dir.east.rawValue + i\n}\nprint(sum)
enum Bit: Int { case zero, one }\nfunc value(_ b: Bit) -> Int { return b.rawValue }\nprint(value(Bit.one) + value(Bit.zero))\nprint(value(Bit.one) * 8)
enum Color { case red, green, blue }\nfunc name(_ c: Color) -> Int {\n  if c == Color.red { return 1 }\n  if c == Color.green { return 2 }\n  return 3\n}\nprint(name(Color.red) + name(Color.green) + name(Color.blue))
enum Tri: Int { case lo, mid, hi }\nlet t = Tri.mid\nif t.rawValue > 0 {\n  print(t.rawValue * 100)\n} else {\n  print(0)\n}
enum Color { case red, green }\nvar hits = 0\nvar i = 0\nwhile i < 10 {\n  var c = Color.green\n  if i % 2 == 0 { c = Color.red }\n  if c == Color.red { hits = hits + 1 }\n  i = i + 1\n}\nprint(hits)
enum Kind: Int { case a, b, c, d, e }\nprint(Kind.e.rawValue - Kind.a.rawValue)\nprint(Kind.c.rawValue == 2)
