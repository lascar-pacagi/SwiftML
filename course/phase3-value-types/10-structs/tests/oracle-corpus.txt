struct Point {\n  var x: Int\n  var y: Int\n}\nlet p = Point(x: 3, y: 4)\nprint(p.x)\nprint(p.y)
struct Point {\n  var x: Int\n  var y: Int\n}\nvar p = Point(x: 1, y: 2)\nvar q = p\nq.x = 99\nprint(p.x)\nprint(q.x)
struct Point {\n  var x: Int\n  var y: Int\n}\nlet p = Point(x: 1, y: 2)\nvar q = p\nq.x = 5\nq.y = q.x * 2\nprint(p.x + p.y)\nprint(q.x + q.y)
struct Point {\n  var x: Int\n  var y: Int\n}\nvar p = Point(x: 1, y: 2)\np = Point(x: 10, y: 20)\nprint(p.x + p.y)
struct Point {\n  var x: Int\n  var y: Int\n}\nstruct Line {\n  var a: Point\n  var b: Point\n}\nfunc mx(_ l: Line) -> Int { return l.b.x }\nprint(mx(Line(a: Point(x: 0, y: 0), b: Point(x: 7, y: 9))))
struct Point {\n  var x: Int\n  var y: Int\n}\nstruct Line {\n  var a: Point\n  var b: Point\n}\nvar l = Line(a: Point(x: 1, y: 2), b: Point(x: 3, y: 4))\nvar m = l\nm.a = Point(x: 9, y: 9)\nprint(l.a.x)\nprint(m.a.x)\nprint(m.b.y)
struct Point {\n  var x: Int\n  var y: Int\n}\nfunc bump(_ p: Point) -> Point { return Point(x: p.x + 1, y: p.y + 1) }\nlet a = Point(x: 1, y: 1)\nlet b = bump(a)\nprint(a.x)\nprint(b.x)\nprint(bump(bump(b)).y)
struct Point {\n  var x: Int\n  var y: Int\n}\nfunc sum(_ p: Point) -> Int { return p.x + p.y }\nvar p = Point(x: 1, y: 2)\nlet s1 = sum(p)\np.x = 100\nprint(s1)\nprint(sum(p))
struct Point {\n  var x: Int\n  var y: Int\n}\nvar p = Point(x: 0, y: 0)\nvar i = 0\nwhile i < 5 {\n  p.x = p.x + i\n  p.y = p.y + 2\n  i = i + 1\n}\nprint(p.x)\nprint(p.y)
struct Point {\n  var x: Int\n  var y: Int\n}\nprint(Point(x: 1, y: 2).x)\nfunc mk() -> Point { return Point(x: 5, y: 6) }\nprint(mk().y)
struct Cell {\n  var n: Int\n  var alive: Bool\n}\nvar c = Cell(n: 0, alive: false)\nc.alive = true\nc.n = 7\nprint(c.alive)\nprint(c.n)\nif c.alive { print(c.n * 2) } else { print(0) }
struct Acc {\n  var total: Int\n  var count: Int\n}\nfunc add(_ a: Acc, _ v: Int) -> Acc { return Acc(total: a.total + v, count: a.count + 1) }\nvar a = Acc(total: 0, count: 0)\nfor i in 1 ..< 6 {\n  a = add(a, i)\n}\nprint(a.total)\nprint(a.count)\nprint(a.total / a.count)
struct Point {\n  var x: Int\n  var y: Int\n}\nfunc far(_ p: Point) -> Bool { return p.x * p.x + p.y * p.y > 25 }\nprint(far(Point(x: 3, y: 4)))\nprint(far(Point(x: 3, y: 5)))\nlet q = Point(x: -3, y: -4)\nprint(far(q))
struct Three {\n  var a: Int\n  var b: Int\n  var c: Int\n}\nvar t = Three(a: 1, b: 2, c: 3)\nlet u = t\nt.c = t.a + t.b\nt.a = t.c * 10\nprint(t.a)\nprint(t.b)\nprint(t.c)\nprint(u.a)\nprint(u.c)
struct Pair {\n  var lo: Int\n  var hi: Int\n}\nfunc swapped(_ p: Pair) -> Pair { return Pair(lo: p.hi, hi: p.lo) }\nvar p = Pair(lo: 1, hi: 9)\nvar n = 0\nwhile n < 3 {\n  p = swapped(p)\n  n = n + 1\n}\nprint(p.lo)\nprint(p.hi)
struct Point {\n  var x: Int\n  var y: Int\n}\nstruct Line {\n  var a: Point\n  var b: Point\n}\nfunc len2(_ l: Line) -> Int {\n  let dx = l.b.x - l.a.x\n  let dy = l.b.y - l.a.y\n  return dx * dx + dy * dy\n}\nprint(len2(Line(a: Point(x: 1, y: 1), b: Point(x: 4, y: 5))))
