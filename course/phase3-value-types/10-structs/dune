; Concept 10 — the complete Phase-2 compiler (given) + structs (value types).
; New work threads `struct` through every stage: types/ast/sil contracts, then the
; parser/sema/silgen/irgen logic.
(library
 (name swiftml_structs)
 (wrapped false)
 (modules token diagnostics types lexer ast parser sema sil silgen irgen driver)
 (flags
  (:standard -warn-error -a)))

(dirs :standard \ solution)
