let a = [1, 2, 3]\nprint(a.map({ (x: Int) -> Int in x * 2 }).count)
let a = [1, 2, 3]\nprint(a.filter({ (x: Int) -> Bool in x > 1 }).count)
let a = [1, 2, 3]\nprint(a.reduce(0, { (s: Int, x: Int) -> Int in s + x }))
let a = [1, 2, 3]\nlet b = a.filter({ (x: Int) -> Int in x * 2 })\nprint(b.count)
let a = [1, 2, 3]\nlet b = a.map({ (s: Bool) -> Int in 1 })\nprint(b.count)
let a = [1, 2, 3]\nlet b = a.reduce(0)\nprint(b)
let a = [1, 2, 3]\nlet b = a.reduce(0, { (s: Int, x: Int) -> Bool in true })\nprint(b)
let a = [1, 2, 3]\nlet b = a.map(5)\nprint(b.count)
let a = [1, 2, 3]\nlet b: [Int] = a.map({ (x: Int) -> Bool in x > 1 })\nprint(b.count)
let a = [1, 2, 3]\nlet n: Int = a.map({ (x: Int) -> Int in x })\nprint(n)
let a = [1, 2, 3]\nprint(a.map({ (x: Int) -> Int in x * 2 }).reduce(0, { (s: Int, x: Int) -> Int in s + x }))
let a = [1, 2, 3]\nlet factor = 10\nprint(a.map({ (x: Int) -> Int in x * factor })[0])
let a = [1, 2, 3]\nprint(a.filter({ (x: Int) -> Bool in x > 1 }).map({ (x: Int) -> Int in x + 1 }).count)
func doubleAll(_ a: [Int]) -> [Int] {\n  return a.map({ (x: Int) -> Int in x + x })\n}\nprint(doubleAll([1, 2]).count)
func doubleAll(_ a: [Int]) -> Int {\n  return a.map({ (x: Int) -> Int in x + x })\n}\nprint(doubleAll([1, 2]))
let a = [1, 2, 3]\nprint(a.reduce(0, { (s: Int, x: Int) -> Int in s + y }))
let a = [1, 2, 3]\nlet f = { (x: Int) -> Int in x * 2 }\nprint(a.map(f).count)
let a = [1, 2, 3]\nprint(a.fold(0, { (s: Int, x: Int) -> Int in s + x }))
let a = [\n  1,\n  2\n]\nprint(a.map({ (x: Int) -> Int in x }).count)
let a = [1, 2, 3]\nprint(a.reduce(0, { (s: Int, x: Int) -> Int in s + x }) + a.filter({ (x: Int) -> Bool in x > 0 }).count)
