var a = [10, 20, 30]\nprint(a.count)\nprint(a[0] + a[2])\na.append(40)\na[1] = 99\nprint(a.count)\nprint(a[1])\nvar sum = 0\nfor x in a { sum = sum + x }\nprint(sum)
let empty: [Int] = []\nprint(empty.count)\nprint(empty.isEmpty)\nvar g = [7]\nprint(g.isEmpty)
var a = [1, 2, 3]\na.append(4)\nvar b = a\nb.append(5)\nb[0] = 99\nprint(a.count)\nprint(a[0])\nprint(b.count)\nprint(b[0])
var a = [1, 2, 3]\nlet b = a\na[0] = 50\nprint(a[0])\nprint(b[0])\nprint(b.count)
var a = [\n  1,\n  2,\n  3\n]\nprint(a.count)\nprint(a[1])\na.append(4)\nprint(a[3])
func total(_ a: [Int]) -> Int {\n  var s = 0\n  for x in a { s = s + x }\n  return s\n}\nvar xs = [100, 20, 3]\nprint(total(xs))\nxs.append(1)\nprint(total(xs))\nprint(xs.count)
func grow(_ a: [Int]) -> Int {\n  var c = a\n  c.append(999)\n  return c.count\n}\nvar xs = [1, 2]\nprint(grow(xs))\nprint(xs.count)
func evens(_ n: Int) -> [Int] {\n  var r: [Int] = []\n  var i = 0\n  while i < n {\n    if i % 2 == 0 { r.append(i) }\n    i = i + 1\n  }\n  return r\n}\nlet e = evens(10)\nprint(e.count)\nprint(e[0] + e[4])
var a: [Int] = []\nvar i = 0\nwhile i < 20 {\n  a.append(i * i)\n  i = i + 1\n}\nprint(a.count)\nprint(a[19])\nvar s = 0\nfor x in a { s = s + x }\nprint(s)
var s = "hello"\nprint(s.count)\nlet t = s + " world"\nprint(t)\nprint(t.count)\nlet u = t + "!"\nprint(u)\nprint(u.count)
var a = [3, 1, 2]\nvar i = 0\nwhile i < a.count {\n  var j = i + 1\n  while j < a.count {\n    if a[j] < a[i] {\n      let tmp = a[i]\n      a[i] = a[j]\n      a[j] = tmp\n    }\n    j = j + 1\n  }\n  i = i + 1\n}\nprint(a[0])\nprint(a[1])\nprint(a[2])
let a = [1, 2, 3]\nprint(a[5])
var a = [1, 2, 3]\na[0 - 1] = 9\nprint(a[0])
struct Bag { var items: [Int] }\nvar b = Bag(items: [1, 2])\nprint(b.items.count)\nprint(b.items[1])
enum Kind { case odd, even }\nfunc kindOf(_ n: Int) -> Kind {\n  if n % 2 == 0 { return Kind.even }\n  return Kind.odd\n}\nvar a = [1, 2, 3, 4]\nvar evens = 0\nfor x in a {\n  if kindOf(x) == Kind.even { evens = evens + 1 }\n}\nprint(evens)
enum E: Error { case empty }\nfunc first(_ a: [Int]) throws -> Int {\n  if a.isEmpty { throw E.empty }\n  return a[0]\n}\ndo { print(try first([7, 8])) } catch { print(0 - 1) }\ndo { print(try first([])) } catch { print(0 - 2) }
func pairSums(_ a: [Int]) -> [Int] {\n  var r: [Int] = []\n  var i = 0\n  while i + 1 < a.count {\n    r.append(a[i] + a[i + 1])\n    i = i + 1\n  }\n  return r\n}\nlet p = pairSums([1, 2, 3, 4])\nprint(p.count)\nprint(p[0])\nprint(p[2])
var a = [1, 2, 3]\nvar b = a\nvar c = b\nc.append(4)\nb[0] = 9\nprint(a[0])\nprint(a.count)\nprint(b[0])\nprint(b.count)\nprint(c[0])\nprint(c.count)
let names = "ab" + "cd"\nvar acc = ""\nvar i = 0\nwhile i < 3 {\n  acc = acc + names\n  i = i + 1\n}\nprint(acc)\nprint(acc.count)
func sumOpt(_ a: [Int]) -> Int? {\n  if a.isEmpty { return nil }\n  var s = 0\n  for x in a { s = s + x }\n  return s\n}\nprint(sumOpt([1, 2, 3]) ?? 0 - 1)\nprint(sumOpt([]) ?? 0 - 1)
