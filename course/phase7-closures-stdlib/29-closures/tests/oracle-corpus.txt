let double = { (x: Int) -> Int in x * 2 }\nprint(double(21))
let isPos = { (x: Int) -> Bool in x > 0 }\nprint(isPos(5))\nprint(isPos(0 - 3))
let show = { (x: Int) -> Void in print(x * 3) }\nshow(14)
func apply(_ f: (Int) -> Int, _ x: Int) -> Int { return f(x) }\nlet inc = { (x: Int) -> Int in x + 1 }\nprint(apply(inc, 41))
func apply(_ f: (Int) -> Int, _ x: Int) -> Int { return f(x) }\nprint(apply({ (x: Int) -> Int in x * x }, 9))
func makeAdder(_ n: Int) -> (Int) -> Int {\n  return { (x: Int) -> Int in x + n }\n}\nlet add7 = makeAdder(7)\nlet add9 = makeAdder(9)\nprint(add7(1))\nprint(add9(1))\nprint(add7(1) + add9(1))
func dbl(_ x: Int) -> Int { return x * 2 }\nlet g = dbl\nprint(g(50))
func sq(_ x: Int) -> Int { return x * x }\nfunc app(_ f: (Int) -> Int, _ v: Int) -> Int { return f(v) }\nprint(app(sq, 9))
func choose(_ b: Bool) -> (Int) -> Int {\n  if b { return { (x: Int) -> Int in x + 1 } }\n  return { (x: Int) -> Int in x - 1 }\n}\nlet up = choose(true)\nlet down = choose(false)\nprint(up(10))\nprint(down(10))
struct P { var x: Int; var y: Int }\nfunc scaler(_ p: P) -> (Int) -> Int {\n  return { (k: Int) -> Int in p.x * k + p.y }\n}\nlet f = scaler(P(x: 3, y: 1))\nprint(f(10))
struct P { var x: Int; var y: Int }\nfunc scaler(_ p: P) -> (Int) -> Int {\n  return { (k: Int) -> Int in p.x * k + p.y }\n}\nvar f = scaler(P(x: 3, y: 1))\nprint(f(10))\nf = scaler(P(x: 5, y: 2))\nprint(f(10))
func twice(_ n: Int) -> Int {\n  let f = { (x: Int) -> Int in x + n }\n  return f(f(0))\n}\nprint(twice(5))
func sumWith(_ f: (Int) -> Int, _ n: Int) -> Int {\n  var s = 0\n  var i = 0\n  while i < n {\n    s = s + f(i)\n    i = i + 1\n  }\n  return s\n}\nprint(sumWith({ (x: Int) -> Int in x * x }, 5))
var i = 0\nvar t = 0\nwhile i < 3 {\n  let f = { (x: Int) -> Int in x + i }\n  t = t + f(10)\n  i = i + 1\n}\nprint(t)
func mk(_ a: Int, _ b: Int, _ c: Int) -> (Int) -> Int {\n  return { (x: Int) -> Int in a * x * x + b * x + c }\n}\nlet q = mk(2, 3, 4)\nprint(q(0))\nprint(q(5))
func pick(_ o: Int?) -> (Int) -> Int {\n  let base = o ?? 100\n  return { (x: Int) -> Int in x + base }\n}\nlet p1 = pick(7)\nlet p2 = pick(nil)\nprint(p1(1))\nprint(p2(1))
class Box {\n  var v: Int\n  init(_ x: Int) { v = x }\n  deinit { print(v) }\n}\nfunc reader(_ n: Int) -> () -> Int {\n  let b = Box(n)\n  let v = b.v\n  return { () -> Int in v * 2 }\n}\nlet r = reader(21)\nprint(r())\nprint(9)
enum Color { case red, green }\nfunc tint(_ c: Color) -> (Int) -> Int {\n  if c == Color.red { return { (x: Int) -> Int in x * 10 } }\n  return { (x: Int) -> Int in x * 100 }\n}\nlet tr = tint(Color.red)\nlet tg = tint(Color.green)\nprint(tr(2))\nprint(tg(2))
struct Pt { let x: Int; var y: Int }\nfunc dist(_ p: Pt) -> () -> Int {\n  return { () -> Int in p.x * p.x + p.y * p.y }\n}\nlet d = dist(Pt(x: 3, y: 4))\nprint(d())
func compose3(_ n: Int) -> Int {\n  let a = { (x: Int) -> Int in x + n }\n  let b = { (x: Int) -> Int in x * 2 }\n  return a(b(a(1)))\n}\nprint(compose3(4))
