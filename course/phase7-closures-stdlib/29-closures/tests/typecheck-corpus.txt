let f = { (x: Int) -> Int in x + 1 }\nprint(f(1))
let f = { (x: Int) -> Int in x + 1 }\nprint(f(1, 2))
let f = { (x: Int) -> Int in x + 1 }\nlet b: Bool = f(1)\nprint(b)
let n = 5\nprint(n(1))
let f: (Int) -> Bool = { (x: Int) -> Int in x + 1 }\nprint(f(1))
func apply(_ f: (Int) -> Int, _ x: Int) -> Int { return f(x) }\nprint(apply(5, 1))
func apply(_ f: (Int) -> Int, _ x: Int) -> Int { return f(x) }\nprint(apply({ (x: Int) -> Int in x * 3 }, 4))
func mk() -> (Int) -> Int { return { (x: Int) -> Bool in x > 0 } }\nlet g = mk()\nprint(g(1))
let f = { (x: Int) -> Int in x + y }\nprint(f(1))
let f = { (x: Int) -> Int in x + 1 }\nf = { (x: Int) -> Int in x + 2 }\nprint(f(1))
var f = { (x: Int) -> Int in x + 1 }\nf = { (x: Int) -> Int in x + 2 }\nprint(f(1))
func g(_ x: Int) -> Int { return x }\nlet h: (Bool) -> Int = g\nprint(h(true))
func g(_ x: Int) -> Int { return x }\nlet h: (Int) -> Int = g\nprint(h(7))
struct P { let x: Int; var y: Int }\nvar p = P(x: 1, y: 2)\np.x = 9\nprint(p.x)
struct P { let x: Int; var y: Int }\nvar p = P(x: 1, y: 2)\np.y = 9\nprint(p.y)
struct P { var x: Int; var y: Int }\nlet a = P(x: 1, y: 2)\nlet b = P(x: 1, y: 2)\nprint(a == b)
func need(_ f: (Int) -> Int) -> Int { return f(1) }\nprint(need({ (s: Bool) -> Int in 1 }))
struct P { var x: Int; var y: Int }\nfunc scaler(_ p: P) -> (Int) -> Int {\n  return { (k: Int) -> Int in p.x * k + p.y }\n}\nlet s = scaler(P(x: 2, y: 1))\nprint(s(3))
let show = { (x: Int) -> Void in print(x) }\nshow(3)
func pick(_ o: Int?) -> (Int) -> Int {\n  let base = o ?? 100\n  return { (x: Int) -> Int in x + base }\n}\nlet p = pick(nil)\nprint(p(1))
