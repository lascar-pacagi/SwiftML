print(#line)\nprint(#line)\nprint(#line)
print(#line + 100)\nlet x = #line\nprint(x)\nfunc g() -> Int { return #line }\nprint(g())
print(#column)\nprint(  #column)\nlet c = #column\nprint(c)
func f() -> Int {\n  return #line\n}\nprint(f())\nprint(#line)
struct S {\n  var x: Int\n  func at() -> Int { return #line }\n}\nlet s = S(x: 1)\nprint(s.at())\nprint(#line)
class C {\n  var n: Int\n  init() { n = #line }\n  func at() -> Int { return #line }\n}\nlet c = C()\nprint(c.n)\nprint(c.at())
var t = 0\nfor i in 0 ..< 3 {\n  t = t + #line\n}\nprint(t)
let a = #line\nlet b = #line\nprint(b - a)\nprint(#line * #column)
if #line > 0 { print(#line) } else { print(0) }
func w(_ n: Int) -> Int { return n * #line }\nprint(w(2))\nprint(w(0))
enum E { case a, b }\nfunc pick(_ e: E) -> Int {\n  switch e {\n  case .a: return #line\n  case .b: return #line\n  }\n}\nprint(pick(E.a))\nprint(pick(E.b))
let xs = [#line, #line, #line]\nprint(xs.count)\nprint(xs[0])\nprint(xs[2])
func opt(_ n: Int) -> Int? {\n  if n < 0 { return nil }\n  return #line\n}\nprint(opt(1) ?? -1)\nprint(opt(-1) ?? -1)
let s = "line "\nprint(s.count + #line)
var n = 0\nwhile n < #line {\n  n = n + 1\n}\nprint(n)
print(#line == 1)\nprint(#column == 7)
func deep() -> Int {\n  let a = #line\n  let b = #column\n  return a * 100 + b\n}\nprint(deep())
let m = [1, 2, 3].map({ (v: Int) -> Int in v + #line })\nprint(m.reduce(0, { (a: Int, b: Int) -> Int in a + b }))
