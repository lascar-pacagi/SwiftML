; Concept 41 — the constraint solver. A LEAF: it forks concept 05's front end rather than
; extending concept 40's, so the solver is the only thing that is new. Nothing depends on it.
; Contracts (given): token diagnostics types lexer ast tast parser constraints csgen driver.
; Skeletons (you edit): cssolver csapply.
(library
 (name swiftml_solver)
 (wrapped false)
 (modules
  token diagnostics types lexer ast tast parser constraints csgen csapply cssolver driver)
 (flags
  (:standard -warn-error -a)))

(dirs :standard \ solution)
