print(1 + 2 * 3)\nprint((1 + 2) * 3)\nprint(20 / 6)\nprint(20 % 6)\nprint(7 - 10)
print(-7 / 2)\nprint(-7 % 3)\nprint(7 % -3)\nprint(7 / -2)\nprint(1 + 2 + 3 + 4)
let x = 1\nlet y = 2\nprint(x + y)\nprint(x * 10 - y)
var s = 0\nfor i in 0 ..< 5 {\n  s = s + i * 2\n  print(s)\n}\nprint(s)
func fib(_ n: Int) -> Int {\n  if n < 2 { return n }\n  return fib(n - 1) + fib(n - 2)\n}\nprint(fib(15))
var t = 0\nfor i in 0 ..< 4 {\n  for j in 0 ..< 4 {\n    if j == i { continue }\n    if j > 2 { break }\n    t = t + i * j\n  }\n}\nprint(t)
var n = 27\nvar steps = 0\nwhile n != 1 {\n  if n % 2 == 0 { n = n / 2 } else { n = 3 * n + 1 }\n  steps = steps + 1\n}\nprint(steps)
struct P {\n  var x: Int\n  var y: Int\n}\nvar p = P(x: 1, y: 2)\nvar q = p\nq.x = 99\nprint(p.x + q.x)\nprint(q.y)
enum Op {\n  case add\n  case mul\n}\nfunc ap(_ o: Op, _ a: Int, _ b: Int) -> Int {\n  switch o {\n  case .add: return a + b\n  case .mul: return a * b\n  }\n}\nprint(ap(Op.add, 3, 4))\nprint(ap(Op.mul, 3, 4))
enum Shape {\n  case sq(Int)\n  case rect(Int, Int)\n}\nfunc area(_ s: Shape) -> Int {\n  switch s {\n  case .sq(let a): return a * a\n  case .rect(let w, let h): return w * h\n  }\n}\nprint(area(Shape.sq(5)))\nprint(area(Shape.rect(3, 4)))
func half(_ n: Int) -> Int? {\n  if n % 2 == 0 { return n / 2 }\n  return nil\n}\nprint(half(8) ?? 0)\nprint(half(7) ?? 0)\nif let v = half(10) { print(v) }
let a = 3 < 5\nprint(a)\nprint(3 == 4)\nprint(a && 5 > 2)\nprint(a || 1 > 2)
if 10 > 3 { print(1) } else { print(2) }\nif 1 == 2 { print(3) } else { print(4) }
var o: Int? = nil\nprint(o!)
func sq(_ x: Int) -> Int { return x * x }\nvar acc = 0\nvar i = 0\nwhile i < 6 {\n  acc = acc + sq(i)\n  i = i + 1\n}\nprint(acc)
