; The concept lab binary: the .t cram tests run ./lab.exe, which links THIS concept's library —
; so the tests are RED until you fill this directory's TODO holes.
(executable
 (name lab)
 (modules lab)
 (libraries swiftml_spec)
 (flags
  (:standard -warn-error -a)))

(cram
 (deps lab.exe oracle-corpus.txt oracle-typecheck.txt))

(tests
 (names test_spec)
 (modules test_spec)
 (libraries alcotest swiftml_spec)
 (flags
  (:standard -warn-error -a)))
