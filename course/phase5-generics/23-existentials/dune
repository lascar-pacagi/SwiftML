; Concept 23 — existential containers: fixed buffer + heap boxing, dynamic casts (as?/as!).
(library
 (name swiftml_existentials)
 (wrapped false)
 (modules token diagnostics types lexer ast parser sema sil silgen opt irgen driver)
 (flags
  (:standard -warn-error -a)))

(dirs :standard \ solution)
